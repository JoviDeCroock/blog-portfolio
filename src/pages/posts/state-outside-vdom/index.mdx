
export const documentProps = {
  title: 'State and VDOM',
  description:
    "We often talk about state in context, ... but this can often be a footgun, let's take a closer look",
  path: '/state-in-vdom',
  tags: ['vdom', 'state', 'front-end']
}

# State in Virtual-DOM

Compare both context and state outside of the Virtual-DOM with granular updates, taking
the example of a form-library might be easier to create a comprehensive example rather than
a data-lib like urql.

## Context

updates grow by consumers

## State outside of the Virtual-DOM

Allows for granularity
