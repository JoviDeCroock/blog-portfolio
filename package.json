{
 "name": "portfolio",
 "type": "module",
 "private": true,
 "author": "Jovi De Croock",
 "description": "My own portfolio",
 "homepage": "https://jovidecroock.com/",
 "license": "MIT",
 "scripts": {
  "prepare": "husky install",
  "dev": "vite",
  "build": "yarn generate && mv src/robots.txt dist/robots.txt && mv src/favicon.ico dist/favicon.ico",
  "build:server": "vite build --ssr src/entry-server.tsx --outDir dist/server && mv dist/server/entry-server.js dist/server/entry-server.cjs",
  "generate": "vite build --outDir dist && yarn build:server && node prerender"
 },
 "dependencies": {
  "@mdx-js/rollup": "^2.1.2",
  "goober": "2.1.10",
  "hoofd": "^1.5.1",
  "preact": "^10.8.2",
  "preact-iso": "^2.3.0",
  "preact-render-to-string": "^5.2.0",
  "preact-ssr-prepass": "^1.2.0",
  "rehype-highlight": "^5.0.2"
 },
 "devDependencies": {
  "@preact/preset-vite": "^2.3.0",
  "husky": "^7.0.4",
  "lint-staged": "^12.3.3",
  "prettier": "^2.5.1",
  "typescript": "^4.5.5",
  "vite": "^2.9.0"
 },
 "lint-staged": {
  "src/**/.tsx": [
   "prettier --write"
  ],
  "src/**/.ts": [
   "prettier --write"
  ],
  "src/**/.mdx": [
   "prettier --write"
  ]
 },
 "husky": {
  "hooks": {
   "pre-commit": "lint-staged"
  }
 }
}
